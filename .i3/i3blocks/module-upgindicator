#!/usr/bin/env bash
#script to check if exist upgrades avaliable
CHECK=$(expr $(apt list --upgradeable | wc -l) - 1)

if [ $CHECK -lt 1 ]; then
    echo ""
else
    echo "<span font='Font Awesome' color='#FF0000'>Upg! ($CHECK)</span>"
fi
